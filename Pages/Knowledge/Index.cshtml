@page
@model IDDQD.Pages.Knowledge.IndexModel

@{
    Layout = null;
}

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElements[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElements[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KnowledgeElements[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.KnowledgeElements) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

<div>
    <a asp-page="./Index">Back to List</a>
    <a asp-page="#contact">Back to Site</a>
</div>
